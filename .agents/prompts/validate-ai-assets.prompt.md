# Validate AI Assets Prompt

Verify that the repository's AI asset layout follows the documented conventions.

Workflow reference:

- `.agents/skills/validate-ai-assets/SKILL.md`

Requirements:

- Check the agent, prompt, skill, and rule directories under `.agents/`.
- Confirm that file names and locations match the repository conventions.
- Run the validator script and resolve any failures before reporting completion.

Output format:

1. Validation status
2. Any problems found
3. Resolution steps taken
